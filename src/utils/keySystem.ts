import { toggleAlfFlag } from "../store/global/global";
import { useAppDispatch } from "./hooks";

// eslint-disable-next-line import/no-anonymous-default-export
export default function (call: (e: KeyboardEvent) => void) {
}